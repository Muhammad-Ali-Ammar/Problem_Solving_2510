---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
		/***************************************** general *********************************************************/
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------




-- 2d array, and sort based on first col 

    vector<vector<int>>& intervals;
    sort(intervals.begin(), intervals.end(),[](const auto& i, const auto& b) { return i[0] < b[0]; });
	
	// witho const & .. it takes much time 
